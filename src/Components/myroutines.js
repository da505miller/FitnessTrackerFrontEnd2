import React, { useEffect, useState } from 'react';
import { useHistory } from 'react-router-dom';
import { userData } from '../API';

const Myroutines = (props) => {

//    const token = props.token;
//    const history = props.history;
//    const routines = props.routines;
//    const setRoutines = props.setRoutines;
//    const activites = props.activites;
//    const setActivities = props.setActivities;

//    const [name, setName] = useState("");
//    const [goal, setGoal] = useState("");
//    const [addActivity, setAddActivity] = ([]);
//    const [userRoutines, setUserRoutines] = ([]);
//    const [user, setUser] = ([]);

//    useEffect(async () => {
//     try {
//         if (token) {
//     const result = await userData(token);
//     console.log("user data is", result);
//     const routines = result.routines;
//     const messages = result.;
//     setUserPosts(posts);
//     setUserMessages(messages);}
//     }
//     catch (err) {
//         console.error("trouble", err)
//     }
    
// }, []);




// }


export default Myroutines;